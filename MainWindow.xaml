<Window
    x:Class="CodexBeacon.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:toolkit="using:CommunityToolkit.WinUI.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="Codex Beacon">

    <Grid Background="{StaticResource CanvasBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="36" />
            <RowDefinition Height="*" />
            <RowDefinition Height="44" />
        </Grid.RowDefinitions>

        <Grid x:Name="AppTitleBar" Grid.Row="0" Background="#EEF3F6" Padding="16,0">
            <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Spacing="9">
                <FontIcon Glyph="&#xE950;" FontSize="14" Foreground="{StaticResource SignalBrush}" />
                <TextBlock Text="CODEX BEACON" FontSize="12" FontWeight="SemiBold" CharacterSpacing="90" Foreground="{StaticResource InkBrush}" />
            </StackPanel>
        </Grid>

        <NavigationView x:Name="Nav" Grid.Row="1" PaneDisplayMode="LeftCompact" IsBackButtonVisible="Collapsed"
                        IsSettingsVisible="False" IsPaneToggleButtonVisible="True" SelectionChanged="Nav_SelectionChanged"
                        Background="Transparent" OpenPaneLength="224" CompactPaneLength="52">
            <NavigationView.MenuItems>
                <NavigationViewItem Content="总览" Tag="dashboard" IsSelected="True">
                    <NavigationViewItem.Icon><FontIcon Glyph="&#xE80F;" /></NavigationViewItem.Icon>
                </NavigationViewItem>
                <NavigationViewItem Content="进程" Tag="processes">
                    <NavigationViewItem.Icon><FontIcon Glyph="&#xE9D9;" /></NavigationViewItem.Icon>
                </NavigationViewItem>
                <NavigationViewItem Content="网络与代理" Tag="network">
                    <NavigationViewItem.Icon><FontIcon Glyph="&#xE968;" /></NavigationViewItem.Icon>
                </NavigationViewItem>
                <NavigationViewItem Content="安装管理" Tag="installations">
                    <NavigationViewItem.Icon><FontIcon Glyph="&#xE896;" /></NavigationViewItem.Icon>
                </NavigationViewItem>
                <NavigationViewItem Content="设置" Tag="settings">
                    <NavigationViewItem.Icon><FontIcon Glyph="&#xE713;" /></NavigationViewItem.Icon>
                </NavigationViewItem>
            </NavigationView.MenuItems>

            <Grid>
                <ScrollViewer x:Name="DashboardPage" Padding="30,22,30,40">
                    <StackPanel MaxWidth="1280" HorizontalAlignment="Stretch">
                        <Grid>
                            <Grid.ColumnDefinitions><ColumnDefinition Width="*" /><ColumnDefinition Width="Auto" /></Grid.ColumnDefinitions>
                            <StackPanel>
                                <TextBlock Text="系统脉冲" Style="{StaticResource PageTitleStyle}" />
                                <TextBlock x:Name="OverallMessageText" Text="正在读取本机 Codex 工具链…" Foreground="{StaticResource MutedBrush}" Margin="0,6,0,0" />
                            </StackPanel>
                            <Button x:Name="RefreshButton" Grid.Column="1" Style="{StaticResource QuietButtonStyle}" Click="Refresh_Click" VerticalAlignment="Center">
                                <StackPanel Orientation="Horizontal" Spacing="8"><FontIcon Glyph="&#xE72C;" FontSize="15" /><TextBlock Text="立即刷新" /></StackPanel>
                            </Button>
                        </Grid>

                        <InfoBar IsOpen="True" Severity="Informational" Title="当前数据路径" Margin="0,18,0,0" IsClosable="False">
                            <StackPanel Spacing="8">
                                <ProgressBar IsIndeterminate="True" Height="3" />
                                <Grid>
                                    <ItemsControl x:Name="PulseHopsList" ItemsSource="{x:Bind ProxyHops}">
                                        <ItemsControl.ItemsPanel><ItemsPanelTemplate><StackPanel Orientation="Horizontal" Spacing="10" /></ItemsPanelTemplate></ItemsControl.ItemsPanel>
                                        <ItemsControl.ItemTemplate>
                                            <DataTemplate>
                                                <StackPanel Orientation="Horizontal" Spacing="8">
                                                    <TextBlock Text="{Binding Name}" FontWeight="SemiBold" VerticalAlignment="Center" />
                                                    <FontIcon Glyph="&#xE76C;" FontSize="12" Foreground="{ThemeResource TextFillColorSecondaryBrush}" VerticalAlignment="Center" />
                                                </StackPanel>
                                            </DataTemplate>
                                        </ItemsControl.ItemTemplate>
                                    </ItemsControl>
                                    <TextBlock x:Name="PulseTimestampText" Text="等待首次采样" HorizontalAlignment="Right" Foreground="{ThemeResource TextFillColorSecondaryBrush}" FontFamily="Cascadia Mono" FontSize="11" />
                                </Grid>
                            </StackPanel>
                        </InfoBar>

                        <TextBlock Text="服务状态" Style="{StaticResource SectionTitleStyle}" />
                        <ItemsControl x:Name="ComponentCards" ItemsSource="{x:Bind Components}">
                            <ItemsControl.ItemsPanel><ItemsPanelTemplate><ItemsWrapGrid Orientation="Horizontal" MaximumRowsOrColumns="3" ItemWidth="360" /></ItemsPanelTemplate></ItemsControl.ItemsPanel>
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <toolkit:SettingsCard Header="{Binding Name}" Description="{Binding Detail}" Width="348" MinHeight="178" Margin="0,0,12,12" IsClickEnabled="False">
                                        <StackPanel Spacing="8" HorizontalAlignment="Stretch">
                                            <Border HorizontalAlignment="Left" Background="{Binding StatusBackground}" CornerRadius="10" Padding="8,3">
                                                <TextBlock Text="{Binding StatusLabel}" Foreground="{Binding StatusForeground}" FontSize="12" FontWeight="SemiBold" />
                                            </Border>
                                            <StackPanel Orientation="Horizontal" Spacing="8">
                                                <TextBlock Text="版本" Foreground="{ThemeResource TextFillColorSecondaryBrush}" FontSize="12" />
                                                <TextBlock Text="{Binding VersionSummary}" FontFamily="Cascadia Mono" FontSize="12" TextTrimming="CharacterEllipsis" />
                                            </StackPanel>
                                            <TextBlock Text="{Binding ReadinessLabel}" Foreground="{ThemeResource AccentTextFillColorPrimaryBrush}" FontSize="12" />
                                            <CommandBar DefaultLabelPosition="Collapsed" IsOpen="False" Background="Transparent" Padding="0">
                                                <AppBarButton Label="安装" Icon="Add" Tag="install" Click="ServiceAction_Click" IsEnabled="{Binding CanInstall}" />
                                                <AppBarButton Label="登录" Icon="Contact" Tag="login" Click="ServiceAction_Click" IsEnabled="{Binding CanLogin}" />
                                                <AppBarButton Label="启动" Icon="Play" Tag="start" Click="ServiceAction_Click" IsEnabled="{Binding CanStart}" />
                                                <AppBarButton Label="停止" Icon="Stop" Tag="stop" Click="ServiceAction_Click" IsEnabled="{Binding CanStop}" />
                                                <AppBarButton Label="重启" Icon="Refresh" Tag="restart" Click="ServiceAction_Click" IsEnabled="{Binding CanRestart}" />
                                                <AppBarButton Label="升级" Icon="Download" Tag="upgrade" Click="ServiceAction_Click" IsEnabled="{Binding CanUpgrade}" />
                                            </CommandBar>
                                        </StackPanel>
                                    </toolkit:SettingsCard>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>

                        <TextBlock Text="Tailscale 概况" Style="{StaticResource SectionTitleStyle}" />
                        <toolkit:SettingsCard Header="Tailscale 网络" IsClickEnabled="False">
                            <toolkit:SettingsCard.Description>
                                <StackPanel><TextBlock x:Name="TailnetSummaryText" Text="正在读取 Tailnet…" /><TextBlock x:Name="TailnetDetailText" Text="设备状态将在这里显示" /></StackPanel>
                            </toolkit:SettingsCard.Description>
                            <Button Tag="network" Click="OpenSection_Click" Content="查看设备" />
                        </toolkit:SettingsCard>
                    </StackPanel>
                </ScrollViewer>

                <ScrollViewer x:Name="ProcessesPage" Visibility="Collapsed" Padding="30,22,30,40">
                    <StackPanel MaxWidth="1280">
                        <TextBlock Text="Codex 进程" Style="{StaticResource PageTitleStyle}" />
                        <TextBlock Text="按父子关系识别桌面应用、CLI、代理、Relay 及其运行时；普通 Node 进程不会被纳入。" Foreground="{StaticResource MutedBrush}" Margin="0,6,0,20" TextWrapping="Wrap" />
                        <Border Style="{StaticResource PanelStyle}" Padding="0">
                            <ListView x:Name="ProcessList" ItemsSource="{x:Bind Processes}" SelectionMode="None">
                                <ListView.Header>
                                    <Grid Padding="18,12" Background="#EEF3F6" ColumnSpacing="14">
                                        <Grid.ColumnDefinitions><ColumnDefinition Width="90" /><ColumnDefinition Width="180" /><ColumnDefinition Width="100" /><ColumnDefinition Width="110" /><ColumnDefinition Width="*" /></Grid.ColumnDefinitions>
                                        <TextBlock Text="PID" FontWeight="SemiBold" /><TextBlock Grid.Column="1" Text="角色 / 进程" FontWeight="SemiBold" /><TextBlock Grid.Column="2" Text="版本" FontWeight="SemiBold" /><TextBlock Grid.Column="3" Text="启动时间" FontWeight="SemiBold" /><TextBlock Grid.Column="4" Text="可执行路径" FontWeight="SemiBold" />
                                    </Grid>
                                </ListView.Header>
                                <ListView.ItemTemplate>
                                    <DataTemplate>
                                        <Grid Padding="10,9" ColumnSpacing="14" ToolTipService.ToolTip="{Binding CommandLine}">
                                            <Grid.ColumnDefinitions><ColumnDefinition Width="90" /><ColumnDefinition Width="180" /><ColumnDefinition Width="100" /><ColumnDefinition Width="110" /><ColumnDefinition Width="*" /></Grid.ColumnDefinitions>
                                            <TextBlock Text="{Binding Pid}" FontFamily="Cascadia Mono" VerticalAlignment="Center" />
                                            <StackPanel Grid.Column="1"><TextBlock Text="{Binding Role}" FontWeight="SemiBold" /><TextBlock Text="{Binding Name}" Foreground="{StaticResource MutedBrush}" FontSize="12" /></StackPanel>
                                            <TextBlock Grid.Column="2" Text="{Binding Version}" FontFamily="Cascadia Mono" VerticalAlignment="Center" />
                                            <TextBlock Grid.Column="3" Text="{Binding StartedAt}" FontFamily="Cascadia Mono" FontSize="12" VerticalAlignment="Center" />
                                            <TextBlock Grid.Column="4" Text="{Binding ExecutablePath}" FontFamily="Cascadia Mono" FontSize="12" TextTrimming="CharacterEllipsis" VerticalAlignment="Center" />
                                        </Grid>
                                    </DataTemplate>
                                </ListView.ItemTemplate>
                            </ListView>
                        </Border>
                        <TextBlock Text="恢复控制" Style="{StaticResource SectionTitleStyle}" />
                        <toolkit:SettingsCard Header="批量恢复可选服务" Description="仅处理 OpenCodex Proxy、Codex Relay 与明确匹配的服务进程；不会关闭 Codex 桌面应用或本管理器。" IsClickEnabled="False">
                            <CommandBar DefaultLabelPosition="Right" Background="Transparent">
                                <AppBarButton Label="终止服务" Icon="Cancel" Tag="kill" Click="DangerAction_Click" />
                                <AppBarButton Label="全部重启" Icon="Refresh" Tag="restart" Click="DangerAction_Click" />
                            </CommandBar>
                        </toolkit:SettingsCard>
                    </StackPanel>
                </ScrollViewer>

                <ScrollViewer x:Name="NetworkPage" Visibility="Collapsed" Padding="30,22,30,40">
                    <StackPanel MaxWidth="1280">
                        <TextBlock Text="网络与代理" Style="{StaticResource PageTitleStyle}" />
                        <TextBlock Text="根据配置文件、环境变量、监听端口和实际进程拼出当前请求路径。敏感凭据不会读取或展示。" Foreground="{StaticResource MutedBrush}" Margin="0,6,0,0" TextWrapping="Wrap" />
                        <TextBlock Text="Codex 代理链" Style="{StaticResource SectionTitleStyle}" />
                        <Border Style="{StaticResource PanelStyle}" Padding="0">
                            <ListView ItemsSource="{x:Bind ProxyHops}" SelectionMode="None">
                                <ListView.ItemTemplate>
                                    <DataTemplate>
                                        <Grid Padding="18,13" ColumnSpacing="14">
                                            <Grid.ColumnDefinitions><ColumnDefinition Width="48" /><ColumnDefinition Width="180" /><ColumnDefinition Width="250" /><ColumnDefinition Width="80" /><ColumnDefinition Width="*" /></Grid.ColumnDefinitions>
                                            <Border Width="32" Height="32" CornerRadius="16" Background="{StaticResource SignalSoftBrush}"><TextBlock Text="{Binding OrderLabel}" HorizontalAlignment="Center" VerticalAlignment="Center" FontFamily="Cascadia Mono" Foreground="{StaticResource SignalBrush}" /></Border>
                                            <TextBlock Grid.Column="1" Text="{Binding Name}" FontWeight="SemiBold" VerticalAlignment="Center" />
                                            <TextBlock Grid.Column="2" Text="{Binding Address}" FontFamily="Cascadia Mono" TextTrimming="CharacterEllipsis" VerticalAlignment="Center" />
                                            <TextBlock Grid.Column="3" Text="{Binding StateLabel}" VerticalAlignment="Center" />
                                            <TextBlock Grid.Column="4" Text="{Binding Evidence}" Foreground="{StaticResource MutedBrush}" TextTrimming="CharacterEllipsis" VerticalAlignment="Center" />
                                        </Grid>
                                    </DataTemplate>
                                </ListView.ItemTemplate>
                            </ListView>
                        </Border>
                        <TextBlock Text="公网出口" Style="{StaticResource SectionTitleStyle}" />
                        <Border Style="{StaticResource PanelStyle}" Padding="0">
                            <ListView ItemsSource="{x:Bind PublicEgressRoutes}" SelectionMode="None">
                                <ListView.ItemTemplate>
                                    <DataTemplate>
                                        <Grid Padding="18,14" ColumnSpacing="16">
                                            <Grid.ColumnDefinitions><ColumnDefinition Width="190" /><ColumnDefinition Width="190" /><ColumnDefinition Width="*" /><ColumnDefinition Width="150" /></Grid.ColumnDefinitions>
                                            <TextBlock Text="{Binding Address}" FontFamily="Cascadia Mono" FontSize="17" FontWeight="SemiBold" />
                                            <TextBlock Grid.Column="1" Text="{Binding Route}" />
                                            <TextBlock Grid.Column="2" Text="{Binding Evidence}" Foreground="{StaticResource MutedBrush}" />
                                            <TextBlock Grid.Column="3" Text="{Binding CheckedAt}" FontFamily="Cascadia Mono" FontSize="12" />
                                        </Grid>
                                    </DataTemplate>
                                </ListView.ItemTemplate>
                            </ListView>
                        </Border>
                        <TextBlock Text="当前外网连接" Style="{StaticResource SectionTitleStyle}" />
                        <Border Style="{StaticResource PanelStyle}" Padding="0">
                            <ListView ItemsSource="{x:Bind ExternalConnections}" SelectionMode="None">
                                <ListView.ItemTemplate>
                                    <DataTemplate>
                                        <Grid Padding="18,12" ColumnSpacing="16">
                                            <Grid.ColumnDefinitions><ColumnDefinition Width="190" /><ColumnDefinition Width="90" /><ColumnDefinition Width="190" /><ColumnDefinition Width="*" /></Grid.ColumnDefinitions>
                                            <TextBlock Text="{Binding Role}" FontWeight="SemiBold" />
                                            <TextBlock Grid.Column="1" Text="{Binding Pid}" FontFamily="Cascadia Mono" />
                                            <TextBlock Grid.Column="2" Text="{Binding ProcessName}" />
                                            <TextBlock Grid.Column="3" Text="{Binding RemoteEndpoint}" FontFamily="Cascadia Mono" />
                                        </Grid>
                                    </DataTemplate>
                                </ListView.ItemTemplate>
                            </ListView>
                        </Border>
                        <TextBlock Text="已配置的备用端点" Style="{StaticResource SectionTitleStyle}" />
                        <Border Style="{StaticResource PanelStyle}" Padding="0">
                            <ListView ItemsSource="{x:Bind CandidateEndpoints}" SelectionMode="None">
                                <ListView.ItemTemplate>
                                    <DataTemplate>
                                        <Grid Padding="18,12" ColumnSpacing="16">
                                            <Grid.ColumnDefinitions><ColumnDefinition Width="190" /><ColumnDefinition Width="300" /><ColumnDefinition Width="*" /></Grid.ColumnDefinitions>
                                            <TextBlock Text="{Binding Name}" FontWeight="SemiBold" />
                                            <TextBlock Grid.Column="1" Text="{Binding Address}" FontFamily="Cascadia Mono" />
                                            <TextBlock Grid.Column="2" Text="{Binding Evidence}" Foreground="{StaticResource MutedBrush}" />
                                        </Grid>
                                    </DataTemplate>
                                </ListView.ItemTemplate>
                            </ListView>
                        </Border>
                        <TextBlock Text="Tailnet 设备" Style="{StaticResource SectionTitleStyle}" />
                        <Border Style="{StaticResource PanelStyle}" Padding="0">
                            <ListView x:Name="TailnetList" ItemsSource="{x:Bind TailnetDevices}" SelectionMode="None">
                                <ListView.ItemTemplate>
                                    <DataTemplate>
                                        <Grid Padding="18,13" ColumnSpacing="14">
                                            <Grid.ColumnDefinitions><ColumnDefinition Width="220" /><ColumnDefinition Width="90" /><ColumnDefinition Width="260" /><ColumnDefinition Width="90" /><ColumnDefinition Width="*" /></Grid.ColumnDefinitions>
                                            <StackPanel><TextBlock Text="{Binding DeviceLabel}" FontWeight="SemiBold" /><TextBlock Text="{Binding DnsName}" Foreground="{StaticResource MutedBrush}" FontSize="12" TextTrimming="CharacterEllipsis" /></StackPanel>
                                            <TextBlock Grid.Column="1" Text="{Binding OS}" VerticalAlignment="Center" />
                                            <TextBlock Grid.Column="2" Text="{Binding Addresses}" FontFamily="Cascadia Mono" FontSize="12" TextTrimming="CharacterEllipsis" VerticalAlignment="Center" />
                                            <TextBlock Grid.Column="3" Text="{Binding OnlineLabel}" VerticalAlignment="Center" />
                                            <TextBlock Grid.Column="4" Text="{Binding LastSeen}" Foreground="{StaticResource MutedBrush}" FontSize="12" VerticalAlignment="Center" />
                                        </Grid>
                                    </DataTemplate>
                                </ListView.ItemTemplate>
                            </ListView>
                        </Border>
                    </StackPanel>
                </ScrollViewer>

                <ScrollViewer x:Name="InstallationsPage" Visibility="Collapsed" Padding="30,22,30,40">
                    <StackPanel MaxWidth="1100">
                        <TextBlock Text="安装管理" Style="{StaticResource PageTitleStyle}" />
                        <TextBlock Text="从 npm 官方注册表获取版本信息。安装与升级会保留现有配置，并在完成后重新检测。" Foreground="{StaticResource MutedBrush}" Margin="0,6,0,20" TextWrapping="Wrap" />
                        <toolkit:SettingsExpander Header="NVM · Node.js 版本控制" Description="管理本机已安装的 Node.js 版本；Codex Relay 需要 Node.js 22.14.0 或更高版本。" IsExpanded="True" Margin="0,0,0,14">
                            <toolkit:SettingsExpander.Items>
                                <toolkit:SettingsCard Header="当前环境" IsClickEnabled="False"><TextBlock x:Name="NvmRequirementText" Text="正在检测 NVM…" TextWrapping="Wrap" /></toolkit:SettingsCard>
                                <toolkit:SettingsCard Header="切换 Node.js" Description="选择 NVM 已安装的版本" IsClickEnabled="False">
                                    <StackPanel Orientation="Horizontal" Spacing="8"><ComboBox x:Name="NodeVersionPicker" Width="210" PlaceholderText="选择已安装版本" ItemsSource="{x:Bind NodeVersions}" DisplayMemberPath="DisplayName" /><Button Content="应用" Click="SwitchNode_Click" /></StackPanel>
                                </toolkit:SettingsCard>
                                <toolkit:SettingsCard Header="安装 Node.js" Description="输入完整版本号，例如 24.15.0" IsClickEnabled="False">
                                    <StackPanel Orientation="Horizontal" Spacing="8"><TextBox x:Name="NewNodeVersionBox" Width="210" PlaceholderText="24.15.0" /><Button Content="安装" Click="InstallNode_Click" /></StackPanel>
                                </toolkit:SettingsCard>
                            </toolkit:SettingsExpander.Items>
                        </toolkit:SettingsExpander>
                        <toolkit:SettingsExpander Header="安装前置条件" Description="在安装或升级 npm 包前验证运行时与包管理器。" IsExpanded="True" Margin="0,0,0,18">
                            <toolkit:SettingsExpander.Items>
                                <toolkit:SettingsCard Header="Node.js" IsClickEnabled="False"><TextBlock x:Name="NodeRequirementText" Text="正在检测版本与路径…" TextWrapping="Wrap" /></toolkit:SettingsCard>
                                <toolkit:SettingsCard Header="npm" IsClickEnabled="False"><TextBlock x:Name="NpmRequirementText" Text="正在检测版本与路径…" TextWrapping="Wrap" /></toolkit:SettingsCard>
                            </toolkit:SettingsExpander.Items>
                        </toolkit:SettingsExpander>
                        <Border Style="{StaticResource PanelStyle}" Padding="0">
                            <ListView x:Name="InstallationList" ItemsSource="{x:Bind InstallableComponents}" SelectionMode="None">
                                <ListView.ItemTemplate>
                                    <DataTemplate>
                                        <Grid Padding="18,16" ColumnSpacing="16">
                                            <Grid.ColumnDefinitions><ColumnDefinition Width="210" /><ColumnDefinition Width="150" /><ColumnDefinition Width="150" /><ColumnDefinition Width="*" /></Grid.ColumnDefinitions>
                                            <StackPanel><TextBlock Text="{Binding Name}" FontSize="16" FontWeight="SemiBold" /><StackPanel Orientation="Horizontal" Spacing="8"><TextBlock Text="{Binding Kind}" Foreground="{StaticResource MutedBrush}" FontSize="12" VerticalAlignment="Center" /><HyperlinkButton Content="{Binding RepositoryLabel}" NavigateUri="{Binding RepositoryUri}" Padding="0" FontSize="12" /></StackPanel></StackPanel>
                                            <StackPanel Grid.Column="1"><TextBlock Text="当前版本" Foreground="{StaticResource MutedBrush}" FontSize="12" /><TextBlock Text="{Binding InstalledVersion}" FontFamily="Cascadia Mono" /></StackPanel>
                                            <StackPanel Grid.Column="2"><TextBlock Text="已知最新版本" Foreground="{StaticResource MutedBrush}" FontSize="12" /><TextBlock Text="{Binding LatestVersion}" FontFamily="Cascadia Mono" /></StackPanel>
                                            <StackPanel Grid.Column="3" Orientation="Horizontal" HorizontalAlignment="Right" Spacing="8">
                                                <Button Content="安装" Tag="install" Click="PackageAction_Click" IsEnabled="{Binding CanInstall}" />
                                                <Button Content="升级" Tag="upgrade" Click="PackageAction_Click" IsEnabled="{Binding CanUpgrade}" />
                                            </StackPanel>
                                        </Grid>
                                    </DataTemplate>
                                </ListView.ItemTemplate>
                            </ListView>
                        </Border>
                        <InfoBar IsOpen="True" Severity="Informational" Title="权限说明" Message="全局 npm 安装可能需要对 Node.js 安装目录的写权限；Relay 服务的计划任务安装可能需要管理员权限。Codex Beacon 会显示明确错误，不会静默提权。" Margin="0,18,0,0" />
                    </StackPanel>
                </ScrollViewer>

                <ScrollViewer x:Name="SettingsPage" Visibility="Collapsed" Padding="30,22,30,40">
                    <StackPanel MaxWidth="760" HorizontalAlignment="Left">
                        <TextBlock Text="设置" Style="{StaticResource PageTitleStyle}" />
                        <TextBlock Text="这些值只描述本机服务，不包含任何密钥。" Foreground="{StaticResource MutedBrush}" Margin="0,6,0,20" />
                        <toolkit:SettingsExpander Header="常规" Description="刷新频率与本机计划任务映射" IsExpanded="True">
                            <toolkit:SettingsExpander.Items>
                                <toolkit:SettingsCard Header="自动刷新间隔" Description="5–300 秒" IsClickEnabled="False"><NumberBox x:Name="RefreshSecondsBox" Width="160" Minimum="5" Maximum="300" SpinButtonPlacementMode="Compact" /></toolkit:SettingsCard>
                                <toolkit:SettingsCard Header="Codex Relay 计划任务" IsClickEnabled="False"><TextBox x:Name="RelayTaskNameBox" Width="300" /></toolkit:SettingsCard>
                                <toolkit:SettingsCard Header="OpenCodex Proxy 计划任务" IsClickEnabled="False"><TextBox x:Name="ProxyTaskNameBox" Width="300" /></toolkit:SettingsCard>
                                <toolkit:SettingsCard Header="保存更改" Description="设置仅存储在本机，不包含凭据。" IsClickEnabled="False"><Button Content="保存" Click="SaveSettings_Click" /></toolkit:SettingsCard>
                            </toolkit:SettingsExpander.Items>
                        </toolkit:SettingsExpander>
                        <TextBlock Text="上游与实现参考" Style="{StaticResource SectionTitleStyle}" />
                        <toolkit:SettingsExpander Header="开源项目" Description="查看组件源码、版本管理方案与网络域名分类数据。" IsExpanded="True">
                            <toolkit:SettingsExpander.Items>
                                <toolkit:SettingsCard Header="OpenCodex" Description="可选 Provider 代理扩展"><HyperlinkButton Content="lidge-jun/opencodex" NavigateUri="https://github.com/lidge-jun/opencodex" /></toolkit:SettingsCard>
                                <toolkit:SettingsCard Header="Codex App Manager" Description="Codex 桌面版本管理实现参考"><HyperlinkButton Content="Wangnov/Codex-App-Manager" NavigateUri="https://github.com/Wangnov/Codex-App-Manager" /></toolkit:SettingsCard>
                                <toolkit:SettingsCard Header="Codex Relay" Description="可选移动端桥接扩展"><HyperlinkButton Content="gronxb/codex-relay" NavigateUri="https://github.com/gronxb/codex-relay" /></toolkit:SettingsCard>
                                <toolkit:SettingsCard Header="domain-list-community" Description="网络域名分类实现参考；当前版本未内置其数据"><HyperlinkButton Content="v2fly/domain-list-community" NavigateUri="https://github.com/v2fly/domain-list-community" /></toolkit:SettingsCard>
                            </toolkit:SettingsExpander.Items>
                        </toolkit:SettingsExpander>
                    </StackPanel>
                </ScrollViewer>
            </Grid>
        </NavigationView>

        <Grid Grid.Row="2" Background="#EEF3F6" Padding="16,0">
            <Grid.ColumnDefinitions><ColumnDefinition Width="Auto" /><ColumnDefinition Width="*" /><ColumnDefinition Width="Auto" /></Grid.ColumnDefinitions>
            <ProgressRing x:Name="BusyRing" Width="18" Height="18" IsActive="False" />
            <TextBlock x:Name="StatusText" Grid.Column="1" Text="准备就绪" VerticalAlignment="Center" Margin="10,0,0,0" Foreground="{StaticResource MutedBrush}" />
            <TextBlock x:Name="RefreshAgeText" Grid.Column="2" Text="尚未刷新" VerticalAlignment="Center" FontFamily="Cascadia Mono" FontSize="12" Foreground="{StaticResource MutedBrush}" />
        </Grid>
    </Grid>
</Window>
